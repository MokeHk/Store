export const addItem = (req, res) => {
  res.send("Add an item.");
};

export const editItem = (req, res) => {
  res.send("Edit an item.");
};

export const sellBuy = (req, res) => {
  res.send("Sell or buy an item here.");
};
